import {
  DivTemplateDirective,
} from './div-template.directive';

describe('DivisionDivDirective', () => {
  it('should create an instance', () => {
    const directive = new DivTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
